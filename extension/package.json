{
  "name": "readaloud-extension",
  "version": "1.0.0",
  "description": "Setup script for ReadAloud Chrome extension",
  "scripts": {
    "setup": "npm install && node setup.js && echo '✓ Done. Load the extension/ folder in Chrome.'"
  },
  "devDependencies": {
    "@mozilla/readability": "^0.5.0",
    "pdfjs-dist": "^3.11.0"
  }
}
